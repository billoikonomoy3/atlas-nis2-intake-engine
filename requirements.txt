# Atlas runtime dependencies (Railway / Nixpacks reads this to select Python).
# The deterministic core needs only pydantic + pyyaml; the API adds fastapi/uvicorn;
# ingestion adds pypdf/python-docx. The extraction model client (anthropic) is an
# OPTIONAL extra — install it only where you set ANTHROPIC_API_KEY.
pydantic>=2.6
pyyaml>=6.0
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
pypdf>=4.0
python-docx>=1.1
# Extraction layer (model calls). Needs ANTHROPIC_API_KEY set at runtime;
# without the key, /extract and /assess/control fail closed with 503.
anthropic>=0.40
